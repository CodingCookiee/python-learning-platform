Write `line_total(quantity, unit_price)` that returns the price of one invoice line: the quantity
times the unit price, rounded to cents (two decimal places).

```python
line_total(3, 4.5)     # 13.5
line_total(3, 0.1)     # 0.3, not 0.30000000000000004
```

Return the number; don't print it.
