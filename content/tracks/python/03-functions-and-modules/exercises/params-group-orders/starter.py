def group_by_customer(orders, groups):
    """Return {customer: [order ids]} for a list of (customer, order_id) pairs.

    groups should be optional: when given, add the orders into it and return it.
    """
    ...
