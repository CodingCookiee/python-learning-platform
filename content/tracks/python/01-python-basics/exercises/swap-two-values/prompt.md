Write `swap(a, b)` that returns the two values in the opposite order.

```python
swap("left", "right")   # ("right", "left")
swap(1, 2)              # (2, 1)
```

You haven't met functions properly yet, so the signature is written for you: fill in the body. A
function sends a value back with `return`, and `return x, y` sends back both values as a pair.
