from decimal import Decimal


def parse_item(line):
    """Turn "Coffee beans, 2, 8.50" into ("Coffee beans", 2, Decimal("8.50"))."""
    ...


if __name__ == "__main__":
    # Read one line, parse it with parse_item, and print "2 x Coffee beans = 17.00"
    ...
