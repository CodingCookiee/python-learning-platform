This order script won't even start: Python reports an `IndentationError`. Fix the indentation so
that:

- when `total` is over 100, it prints `Free shipping` and then `Your order qualifies`,
- it always prints `Order total: 150` at the end.

Don't change any of the text, only the spaces at the start of lines.
