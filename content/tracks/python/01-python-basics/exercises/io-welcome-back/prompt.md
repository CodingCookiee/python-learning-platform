Write a program that asks for the customer's name and greets them:

```text
Your name: Ada
Welcome back, Ada!
```

People sometimes type spaces before or after their name, so remove them: `"  Grace "` should give
`Welcome back, Grace!`. Pass the question to `input()` rather than printing it, so that the greeting
is the only line your program prints.
