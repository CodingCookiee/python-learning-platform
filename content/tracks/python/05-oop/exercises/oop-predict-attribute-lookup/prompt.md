A subscription plan has a default price on the class, and one customer gets a custom price. Follow
every read and write: which ones touch the instance, and which the class? Type exactly what the
program prints.
