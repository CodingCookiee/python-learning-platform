class Product:
    def __init__(self, name, price, stock=0):
        self.name = name
        self.price = price
        self.stock = stock

    # Add the price and stock properties (each with a setter), and in_stock
