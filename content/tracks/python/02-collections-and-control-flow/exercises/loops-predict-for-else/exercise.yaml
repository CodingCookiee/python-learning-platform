title: "Predict: break, continue and else"
type: predict
difficulty: core
tags: [loops, break, continue, for-else, while]
hints:
  - continue skips the rest of the body for that bin only. break ends the whole loop, and then the else is skipped.
  - The second loop has no break, so its else runs. The while loop checks n before each pass, and n goes 3, 1, -1.
