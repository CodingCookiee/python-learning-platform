Write `numbered(tasks)` that turns a list of tasks into a numbered checklist, counting from 1:

```python
numbered(["Back up database", "Rotate logs", "Renew certificate"])
# ["1. Back up database", "2. Rotate logs", "3. Renew certificate"]
```

Return a new list of strings. An empty list of tasks gives an empty checklist.
