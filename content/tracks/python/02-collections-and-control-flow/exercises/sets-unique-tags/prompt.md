A blog's tag cloud needs every tag once, in alphabetical order. Write `unique_tags(tags)` that returns
a sorted list with the duplicates removed:

```python
unique_tags(["python", "web", "python", "api", "web"])   # ["api", "python", "web"]
```
