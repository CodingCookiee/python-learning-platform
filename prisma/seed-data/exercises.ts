// Exercise seed data for Modules 1-3

export function getExercisesForLesson(lessonId: string, moduleOrder: number, lessonOrder: number) {
  // For MVP, return empty array - exercises will be added in next iteration
  // This allows the seed script to run and populate modules/lessons/projects first
  return [];
}
